# 2026-09-26

**Outcome:** Built F4, which downloads the Sample Labels `.docx` from a flavor sheet. Opened PR #52. It needs your Word test print and a merge.

## Queue at start of run
GitHub showed no open PRs. Master was at 8cf6bfb.

## What I did
The first unchecked box was **F4. Labels**.
- Added `dtf_materials/labels_docx.py`, stdlib only. It gives one label per flavor, ten to a document, values only. It copies label 1's paragraphs into each label it uses and never touches the shapes or row heights.
- Added a `product_scoops` table. A product with no stored count uses 1 scoop, and a changed count is remembered for that product.
- The Flavor Sheet tab gains a **Scoops per serving** field and one **Download Labels (.docx)** button per 10 flavors.
- Ran `python -m pytest`: 182 passed, 1 skipped (the existing gspread test). That includes 14 new label tests and 1 new smoke test.
- Ran the synthetic generator, the ETL and the quality report. All three finished cleanly.
- I could not open the output in Word or LibreOffice. LibreOffice here would not load any file, not even a `.txt`.

## PR opened
#52, "Fill the Sample Labels template from a flavor sheet (F4)", on branch `claude/quirky-pasteur-lpld0i`, based on `master` because nothing was open to stack on.

## Judgment calls
- I ticked F4, but part of its DoD is you checking the labels in Word and on a test print. If the grid is off, un-tick it.
- If BASE or any line's mg is blank, the serving size prints blank. A weight with a missing amount would be too low.
- Scoops are remembered by product name, not by customer and product, because SPEC says "per product".
- The scheduled prompt lists the phases as A→B→E→D. SPEC §6 says A→B→E→D→F. I followed §6, so the prompt needs updating to match.

## Needs you
- Copy `Sample Labels Blank Template.docx` into `data/real/templates/`. Then download labels for a real flavor sheet, open the file in Word, and do a test print on the label stock.
- Merge #52.
