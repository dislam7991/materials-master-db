# 2026-09-17

**Outcome:** Fourth consecutive out-of-work run. Section 4 is complete on
`master`, the queue is empty. Section 6's finished-checklist rule applied: no
code touched, log written, no task invented.

## Queue at start of run

**Empty.** No open PRs on GitHub. Since yesterday's entry three PRs merged, all
interactive, none of them section 4 work:

- **#30** — condensed SPEC.md (891 lines changed, mostly deletions). This is
  why the section numbering moved: the old §7 "Working agreement" is now
  **section 6**, Status is **section 4**, out-of-scope is **section 5**. Every
  `§7.x` reference in prior run logs points at the pre-condense file.
- **#31** — added the formula-builder direction (Phase F) to the condensed
  SPEC. Phase F carries no `- [ ]` boxes and the automation skips it, same as
  Parked Phase C.
- **#32** — revised the commit-authorship format and stopped section 6 pinning
  a stale `Co-Authored-By` trailer. This resolves the §7.5 mismatch carried in
  the "Needs you" of every run back to 2026-09-09.

Nothing broken, nothing stacked, so section 6's order-of-work rules 1 and 3
fell through to rule 2 and the next unchecked box was looked for.

## What I did

Nothing to the code. Checked `origin/master` at `ccfb768`: section 4 has
**zero** unchecked `- [ ]` boxes across Phases A, B, E and D. The only `- [ ]`
boxes in the file are C1–C4 in Parked Phase C, which the automation skips. That
is section 6's finished-checklist rule exactly.

Verification run: none, because nothing changed. Working tree clean at
`ccfb768`; the only write this run is this file.

## PR opened

**None.** No unchecked box to take, and section 6 forbids inventing one.

## Judgment calls

- **Prompt vs. section 6: they agree on substance** — order of work, the
  3-PR queue cap, branch choice, the log going straight to `master`, one Slack
  message, and both land on the finished-checklist rule for a complete
  section 4. No mismatch to report. The stale-trailer mismatch that was the
  only standing disagreement is now gone (#32).
- **Did not treat #30/#31/#32 as automation work.** All interactive, all merged,
  none ticks a section 4 box; recorded only so the numbering shift and the
  resolved trailer are traceable.
- **Did not promote anything from Direction Phase F or Parked Phase C.** Both
  are held deliberately and carry no `- [ ]` boxes; moving a task into
  section 4 is the user's call in a reviewed PR, not the automation's.
- **Nothing added to the Backlog.** No task attempted, so nothing came up.

## Needs you

**The automation is out of work and will stay out of work.** Every run from
here finds a complete section 4, writes a log and stops. To restart it, move a
task into section 4 in a reviewed PR. Standing candidates, unchanged for four
runs and both from the pre-condense Phase E notes:

1. A lab-specific quality report, or an in-app warnings tab surfacing flagged
   issues (duplicate sample codes, cross-vendor collisions) the way the main
   inventory's quality report does.
2. The open cleaning gaps: no parser for the lab's `A-2-1`-style location codes
   (let alone the plain-English ones), and declaration-type tags whose order
   varies, so `Natural, WONF` and `WONF, Natural` still read as two categories.

Unparking Phase C is the other option, if material usage history is ever
actually wanted. Phase F (formula builder), newly written into the SPEC, is a
larger direction whose prerequisite is real templates only you can supply.

The §7.5 trailer mismatch is **resolved** (#32) and drops off this list.
