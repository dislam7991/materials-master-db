# 2026-09-21

**Outcome:** Took F1 (the formula object) — new `formulas` + `formula_lines`
schema and totals — and opened PR #35. Queue was empty; nothing broken.

## Queue at start of run
No open automation PRs on GitHub. Master at #34 (Phase F promoted into section 4
the day before). Section 4 is no longer complete — Phase F (F1–F4) is now the
active queue.

## What I did
First unchecked box, top to bottom: **F1. Formula object**. Not blocked (schema,
init, and tests are all synthetic-buildable now). Added `formulas` and
`formula_lines` to `db/schema.sql` (a line references `material_id` or the
stable `rd_id`, never copies name/price; a CHECK + two FKs pin exactly one real
reference per line), a `dtf_materials/formulas.py` module (create/view/total,
prices resolved at read time), and `tests/test_formulas.py`. Ticked the F1 box
in the same commit.

Verification actually run: `python -m pytest` → **121 passed, 1 skipped** (9 new
formula tests, including a repriced material flowing through an existing line and
an ETL reload leaving formula rows untouched). Full CI pipeline (generator →
ETL → quality report) ran clean.

## PR opened
#35 — "Add the formula object: formulas + formula_lines schema and totals (F1)",
branch `claude/focused-ritchie-uek4xb`, based on **master** (F1 is independent;
no PR to stack on).

## Judgment calls
- Read helpers (`get_lines`, `formula_total`) live in `formulas.py`, not
  `queries.py`, so F1 is one cohesive object; F2 can relocate them per its DoD.
- A formula total is a floor when a line lacks a price: uncosted lines are
  excluded rather than counted as an invented zero (SPEC §2), and `get_lines`
  exposes the NULL price so a UI can flag them.
- No Backlog entries added.

## Needs you
Review and merge PR #35 so F2 (the builder UI) can be taken next.
