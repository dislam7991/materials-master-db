# 2026-09-22

**Outcome:** Took F2 (the formula builder UI) and opened PR #36. Queue was
empty; nothing broken.

## Queue at start of run
No open automation PRs on GitHub. Master at #35 (F1, the formula object, merged
since the 2026-09-21 run). Phase F is the active queue: F1 `[x]`, F2 next.

## What I did
First unchecked box, top to bottom: **F2. Formula builder UI**. Not blocked (it
builds against synthetic warehouse materials, no real files or credentials).
Added a **Formula builder** tab to `app.py` — pick/start a formula, search the
warehouse or lab catalog, add a *picked* row (never typed) with an amount, view
the lines and total. One new read, `formulas.list_formulas`, for the picker;
`get_conn` now enables `foreign_keys` and writes go through `formulas.py` (which
commits its own inserts). Ticked the F2 box in the same commit.

Verification actually run: `python -m pytest` → **123 passed, 1 skipped** (2 new
in `tests/test_formulas.py`: `list_formulas` ordering/counts, and a formula
built end-to-end from synthetic-loaded materials with its total asserted against
catalog prices). Full CI pipeline (generator → ETL → quality report) ran clean;
`py_compile app.py` OK.

## PR opened
#36 — "Add the formula builder UI (F2)", branch
`claude/focused-ritchie-898lkr`, based on **master** (F1 is merged, so nothing
to stack on).

## Judgment calls
- Formula data access stays in `formulas.py`, not `queries.py` as F2's DoD line
  says — that line predates F1 giving the object its own module, and moving it
  would be churn. The real intent (no SQL in the UI) is honored: `app.py` reads
  only through `formulas.py` + `queries.py`. Flagged in the PR for the reviewer.
- No line removal/editing this pass — the DoD is build + view + total; delete
  needs a new `formulas.py` function and is out of scope.
- Streamlit isn't a CI/dev dependency, so `app.py` stays UI-only and untested by
  pytest (repo convention); the DoD is proven through the data path the UI
  drives.
- No Backlog entries added.

## Needs you
Review and merge PR #36 so F3/F4 can be taken next. Note F3 is **blocked** —
it needs copies of the three real templates (flavor sheet, sample record sheet,
labels) from you before any renderer work can start.
